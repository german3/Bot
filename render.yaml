services:
  - type: web
    name: whatsapp-bot
    env: docker
    plan: free
    disk:
      name: data-disk
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: DATA_DIR
        value: /data
      - key: PORT
        value: 3000
